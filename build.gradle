buildscript {
  repositories {
    mavenCentral()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:0.13.0'
  }
}

apply plugin: 'com.android.application'

android {
  compileSdkVersion 20
  buildToolsVersion "20.2.0"
  sourceSets {
    main {
      manifest.srcFile 'AndroidManifest.xml'
      java.srcDirs = ['src/main']
      res.srcDirs = ['res']
    }
  }
}

android {
      lintOptions {
          abortOnError false
      }
  }

